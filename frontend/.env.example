# ggBoard frontend environment — EXAMPLE (safe to commit)
# Copy to .env:  cp .env.example .env
# Base URL of the ggBoard backend API (must be reachable from the browser).
VITE_API_URL=http://localhost:3001
